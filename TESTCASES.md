# RobotSim 多引擎机器人仿真平台功能验收测试用例

## 1. 测试概述

本验收测试用例定义了 RobotSim 作为 OpenHarmony 原生具身智能仿真平台的准出标准，确保核心功能与性能指标满足设计要求。测试覆盖多引擎兼容性、OpenHarmony 端侧协同、API 一致性等关键维度，作为每个版本发布的强制性验证标准。

## 2. 核心抽象层功能验证

### 2.1 基础实体操作
- **测试场景**：创建标准桌面抓取环境
- **测试步骤**：
  1. 调用 `sim.create_scene("table_top")`
  2. 使用 `sim.spawn_entity("cube", position=(0.5, 0, 0.1))` 创建立方体
  3. 通过 `sim.raycast(origin=(0.5,0,1), direction=(0,0,-1))` 验证物体存在
  4. 执行 `sim.set_joint_state(robot_id, joint_name="arm_joint", value=0.75)`
- **验收标准**：
  - 量个支持引擎（MuJoCo/Gazebo）均能正确执行相同 API
  - 实体位置误差 ≤ 0.01m
  - 关节状态设置误差 ≤ 0.005 radians
  - API 调用延迟 ≤ 5ms

### 2.2 传感器数据一致性
- **测试场景**：RGB-D 相机数据采集
- **测试步骤**：
  1. 调用 `camera = sim.spawn_sensor("rgbd_camera", position=(1,0,1))`
  2. 获取连续 10 帧传感器数据
  3. 在三个引擎中分别执行相同操作
- **验收标准**：
  - 各引擎返回数据结构一致
  - 相同场景下深度值差异 ≤ 5%
  - 640x480 分辨率下帧采集延迟 ≤ 50ms

## 3. OpenHarmony 端侧协同测试

### 3.1 多模态设备控制
- **测试场景**：使用手机控制仿真机械臂
- **测试步骤**：
  1. 通过 OpenHarmony DSoftBus 连接手机与仿真平台
  2. 在手机上执行倾斜操作（使用陀螺仪）
  3. 通过触摸屏绘制轨迹
  4. 语音命令"抓取红色方块"
- **验收标准**：
  - 端云数据同步延迟 ≤ 10 ms
  - 语音命令识别准确率 ≥ 90%
  - 轨迹跟踪误差 ≤ 0.05 m

### 3.2 分布式数据采集
- **测试场景**：多设备协同数据收集
- **测试步骤**：
  1. 配置注解作为主仿真节点
  2. 连接 OH EDU 作为辅助传感器
  3. 执行 50 次抓取动作，采集多视角数据
  4. 验证数据集完整性
- **验收标准**：
  - 数据同步时间戳误差 ≤ 5 ms
  - 50 次动作中数据丢失率 ≤ 10 %
  - 采集吞吐量 ≥ 30 fps/设备

## 4. 自然语言环境构建 (NL2Sim)

### 4.1 语义场景生成
- **测试场景**：自然语言描述生成仿真环境
- **测试步骤**：
  1. 输入指令："创建一个厨房场景，包含灶台、水槽和冰箱，桌面上有红色苹果和蓝色杯子"
  2. 调用 NL2Sim 模块解析指令
  3. 验证生成环境与指令一致性
- **验收标准**：
  - 关键元素识别准确率 ≥ 85%
  - 位置关系错误率 ≤ 10%
  - 生成时间 ≤ 8秒

## 5. ROS2 集成验证

### 5.1 基本通信桥接
- **测试场景**：DDS 与 gRPC 在 OpenHarmony 设备通信
- **测试步骤**：
  1. 启动 RobotSim 仿真环境
  2. 通过 gRPC 接口创建 DDS 桥接服务
  3. 启动 ROS2 节点发布 `/tf` 坐标变换数据
  4. 在仿真环境中订阅并验证数据
- **验收标准**：
  - 消息端到端延迟 ≤ 15ms
  - 10,000 条连续消息传输无丢失
  - 数据格式转换正确率 100%
  - 桥接服务持续运行 1 小时稳定性 ≥ 99.9%

### 5.2 MoveIt2 运动规划集成
- **测试场景**：通过 MoveIt2 控制仿真机械臂
- **测试步骤**：
  1. 在 RobotSim 中加载 Franka 机械臂模型
  2. 通过 ROS2 桥接初始化 MoveIt2 规划场景
  3. 设置障碍物并请求从 A 点到 B 点的无碰撞轨迹
- **验收标准**：
  - 规划请求成功率 ≥ 95%
  - 轨迹执行成功率 ≥ 90%
  - 规划-执行端到端延迟 ≤ 100 ms
  - 各引擎下轨迹执行位置误差 ≤ 0.1 m
  - 支持至少 2 种常见机械臂模型 (Franka, XLRobot)

### 5.3 Navigation2 导航能力
- **测试场景**：2D 导航与避障
- **测试步骤**：
  1. 在 RobotSim 中创建 10 m × 10 m 室内环境
  2. 生成占位栅格地图 pgm
  3. 通过 Navigation2 栈连续设置 3 个目标点
  4. 动态添加移动障碍物
- **验收标准**：
  - SLAM 建图准确率 ≥ 95%（与预设环境对比）
  - 路径规划成功率 ≥ 90%
  - 动态避障成功率 ≥ 85%
  - 导航定位误差 ≤ 0.1m
  - 支持至少 Gazebo 引擎下的导航功能
  - 导航命令端到端延迟 ≤ 100ms

### 5.4 ROS2 组件生命周期管理
- **测试场景**：ROS2 节点生命周期控制
- **测试步骤**：
  1. 通过 RobotSim API 启动 ROS2 导航节点
  2. 执行导航任务
  3. 调用 API 停止 ROS2 节点
  4. 验证资源释放
- **验收标准**：
  - 节点启动时间 ≤ 1s
  - 节点停止时间 ≤ 500ms
  - 100 次启停循环无内存泄漏
  - 资源释放完全（CPU/内存占用回归基准值）

## 6. 真实物理本体机器人验证

### 6.1 Sim2Real 迁移
- **测试场景**：从仿真到物理机器人的策略迁移
- **测试步骤**：
  1. 在 RobotSim 中训练抓取策略
  2. 通过 ROS2 桥接将策略部署到 OpenHarmony 驱动的物理机器人
  3. 在相同场景下执行 100 次抓取任务
  4. 记录成功率和性能指标
- **验收标准**：
  - Sim2Real 迁移成功率 ≥ 50%
  - 物理执行延迟 ≤ 50ms
  - 100 次任务中安全操作率 100%（无碰撞/超限）

### 6.2 真实本体联调
- **测试场景**：物理-仿真联合调试
- **测试步骤**：
  1. 启动物理机器人与仿真环境同步
  2. 在仿真环境中修改参数
  3. 观察物理机器人响应
  4. 反向操作：在物理机器人上执行动作，观察仿真状态更新
- **验收标准**：
  - 联合调试连接稳定性 ≥ 99%
  - 参数同步延迟 ≤ 20ms
  - 状态同步误差 ≤ 0.1 m（位置）/ ≤ 0.1 rad（姿态）

## 7. VLA 模型集成与 LeRobot 数据采集验证

### 7.1 数据采集管道
- **测试场景**：使用 LeRobot 采集仿真数据
- **测试步骤**：
  1. 配置 LeRobot 兼容数据采集管道
  2. 启动标准抓取任务 100 次
  3. 采集多视角 RGB、深度图、关节状态、动作指令
  4. 验证数据格式和完整性
- **验收标准**：
  - 数据格式符合 LeRobot 标准
  - 100 次任务数据完整率 ≥ 98%
  - 采集速率 ≥ 30fps
  - 视角同步误差 ≤ 5ms

### 7.2 VLA 模型推理
- **测试场景**：集成开源 VLA 模型执行任务
- **测试步骤**：
  1. 集成开源 VLA 模型（如 OpenVLA）
  2. 输入自然语言指令："拿起左边的红色方块"
  3. 提供当前环境观测数据
  4. 执行模型生成的控制序列
- **验收标准**：
  - 指令理解准确率 ≥ 85%
  - 任务成功率 ≥ 80%（在简单场景）
  - 端到端推理延迟 ≤ 200ms
  - 能在至少 2 种不同仿真引擎中工作

## 8. 资源效率与性能

### 8.1 资源占用
- **测试环境**：OpenHarmony 标准系统，4GB RAM，4 核 CPU，L2 设备以上
- **测试步骤**：
  1. 启动基础仿真环境
  2. 逐步添加复杂度（实体、传感器）
  3. 监控资源使用情况
- **验收标准**：
  - 基础模块内存占用 ≤ 50MB
  - 中等复杂度场景 (10 实体、3 传感器) CPU使用率 ≤ 60%

### 8.2 帧率性能
- **测试场景**：标准抓取任务
- **测试步骤**：
  1. 运行 1000 帧仿真
  2. 测量平均帧时间和稳定性
- **验收标准**：
  - 中等复杂度场景平均帧率 ≥ 30FPS
  - 帧时间标准差 ≤ 2ms
  - 长时间运行 (1 小时) 无明显性能下降

## 9. 准出标准汇总

| 测试类别   | 必过项数量 | 高优先级项数量 | 通过标准                     |
| ---------- | ---------- | -------------- | ---------------------------- |
| 核心功能   | 2          | 0              | 100% 通过                    |
| 引擎兼容   | 1          | 0              | 100% 通过                    |
| 端侧协同   | 2          | 0              | 100% 通过                    |
| 自然语言   | 0          | 1              | ≥80% 通过                    |
| ROS2 集成  | 2          | 2              | 必过项100%通过，高优先级≥80% |
| 物理机器人 | 1          | 1              | 必过项100%通过，高优先级≥70% |
| VLA 集成   | 0          | 2              | ≥75% 通过                    |
| 性能指标   | 1          | 1              | 100% 通过                    |

**版本发布准出条件**：
- “必过项”测试 100% 通过
- “高优先级项” 测试平均通过率 ≥80%
- 任一性能指标不达标的测试项 ≤ 2 项
- 无 P0 级(系统崩溃/数据丢失)缺陷

**测试报告要求**：
- 提供自动化测试脚本执行结果
- 附关键 API 的调用示例与输出验证
- 提供 VLA 模型集成示例代码和 LeRobot 数据格式映射文档
- 提供物理机器人连接和联合调试的完整操作指南
- **ROS2 集成专项**：
  - 提供 MoveIt2 与 Navigation2 的兼容性矩阵
  - 提供 ROS2 通信带宽与延迟基准测试数据

